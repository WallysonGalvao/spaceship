// Storage
const appName = '@spaceship:';

export const STORAGE_AUTH = `${appName}auth`;
export const STORAGE_USER = `${appName}user`;
export const STORAGE_MISSIONS = `${appName}missions`;
export const STORAGE_PLANETS = `${appName}planets`;
